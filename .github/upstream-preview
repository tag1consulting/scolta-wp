# Ref of an UNMERGED scolta-php branch to run this repo's suite against.
# "main" (or an absent file) means there is nothing unmerged to preview and the
# job skips: the required jobs already test against dev-main.
main
